import { makeStyles } from "@material-ui/core";

export const SigninStyles = makeStyles((theme) => ({
  userInput: {
    width: "100%",
  },
  saveButton: {
    width: "100%",
    textAlign: "center",
    margin: "32px auto",
  },
}));
