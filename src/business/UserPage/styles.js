import { makeStyles } from "@material-ui/core";

export const UserPageStyles = makeStyles((theme) => ({
  userInput: {
    width: "100%",
  },
  saveButton: {
    width: "110px",
    marginTop: "32px",
  },
}));
